"""Page modules for the diabetes portal.

Each module exposes a `render(model)` function that draws the page. Pages that
don't need the model still accept it (and ignore it) so the router can call
them uniformly.
"""
